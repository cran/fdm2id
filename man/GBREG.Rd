% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classification.R
\name{GBREG}
\alias{GBREG}
\title{Regression using Gradient Boosting}
\usage{
GBREG(
  x,
  y,
  ntree = 500,
  learningrate = 0.3,
  tune = FALSE,
  methodparameters = NULL,
  graph = FALSE,
  seed = NULL,
  ...
)
}
\arguments{
\item{x}{Predictor values of the training set, as a \code{matrix} or \code{data.frame}.}

\item{y}{Target values of the training set (a numeric \code{vector}).}

\item{ntree}{The number of trees in the ensemble.}

\item{learningrate}{The learning rate (between 0 and 1).}

\item{tune}{If true, the function returns parameters instead of a classification model.}

\item{methodparameters}{Present for interface consistency with \code{\link{performance}}
(which always passes it when fitting a model). Currently unused: \code{GBREG} does not yet
implement hyperparameter tuning.}

\item{graph}{Present for interface consistency with \code{\link{performance}} (which always
passes it when fitting a model). Currently unused: \code{GBREG} does not produce a plot.}

\item{seed}{A specified seed for random number generation (row/column subsampling, if used
via \code{...}).}

\item{...}{Other parameters, passed to \code{\link[xgboost]{xgboost}}.}
}
\value{
The regression model.
}
\description{
This function builds a regression model using Gradient Boosting. It is the regression
counterpart of \code{\link{GRADIENTBOOSTING}}, which classifies.
}
\examples{
\donttest{
require (datasets)
data (trees)
d = splitdata (trees, 3)
model = GBREG (d$train.x, d$train.y)
evaluation (predict (model, d$test.x), d$test.y)
}
}
\seealso{
\code{\link{GRADIENTBOOSTING}}, \code{\link{LINREG}}, \code{\link{SVR}},
\code{\link[xgboost]{xgboost}}
}
