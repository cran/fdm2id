% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/text.R
\name{vectorize.words}
\alias{vectorize.words}
\title{Word vectorization}
\usage{
vectorize.words(
  corpus = NULL,
  ndim = 50,
  maxwords = NULL,
  mincount = 5,
  minphrasecount = NULL,
  window = 5,
  maxcooc = 10,
  maxiter = 10,
  epsilon = 0.01,
  lang = "en",
  stopwords = lang,
  excludewords = NULL,
  removesinglechars = TRUE,
  ...
)
}
\arguments{
\item{corpus}{The corpus of documents (a vector of characters).}

\item{ndim}{The number of dimensions of the vector space.}

\item{maxwords}{The maximum number of words.}

\item{mincount}{Minimum word count to be considered as frequent.}

\item{minphrasecount}{Minimum collocation of words count to be considered as frequent.}

\item{window}{Window for term-co-occurrence matrix construction.}

\item{maxcooc}{Maximum number of co-occurrences to use in the weighting function.}

\item{maxiter}{The maximum number of iteration to fit the GloVe model.}

\item{epsilon}{Defines early stopping strategy when fit the GloVe model.}

\item{lang}{The language of the documents (NULL if no stemming).}

\item{stopwords}{The language whose stop words are removed (\code{"en"}, ...), or
\code{NULL} to keep them. A list of words of your own goes to \code{excludewords}.}

\item{excludewords}{An optional custom vector of additional words to exclude from the vocabulary (e.g. corpus-specific stop words), on top of (or instead of) the language stopwords given through \code{stopwords}.}

\item{removesinglechars}{Whether single-character tokens are removed during cleanup.}

\item{...}{Other parameters.}
}
\value{
The vectorized words.
}
\description{
Vectorize words from a corpus of documents.
}
\examples{
\donttest{
# 'capitals' is small, so the word vectors are coarse and 'ndim' is reduced
# accordingly; phrase detection needs a much larger corpus.
data (capitals)
words = vectorize.words (capitals, mincount = 2, ndim = 10, maxiter = 5)
query.words (words, origin = "paris", sub = "france", add = "germany")
query.words (words, origin = "berlin", sub = "germany", add = "france")
}
}
\seealso{
\code{\link{query.words}}, \code{\link[stopwords]{stopwords}}, \code{\link[text2vec]{vectorizers}}
}
